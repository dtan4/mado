#!/usr/bin/env ruby

require "mado"

app = Rack::Builder.app do
  map "/assets" do
    run Mado::App.sprockets
  end

  map "/" do
    run Mado::App
  end
end

options = { app: app }

Rack::Server.start(options)
